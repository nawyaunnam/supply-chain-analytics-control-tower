name: supply_control_tower
version: '1.0.0'
config-version: 2
profile: supply_control_tower
model-paths: [models]
test-paths: [tests]
macro-paths: [macros]
models:
  supply_control_tower:
    staging:
      +materialized: view
    marts:
      +materialized: table
    intelligence:
      +materialized: table
      +tags: [intelligence]
flags:
  send_anonymous_usage_stats: false
